#   Exemplo utilizando actuator

Projeto contém exemplos de como configurar Spring Actuator
 
 
## Build
 
Para efetuar o build da aplicação, execute o comando abaixo, no terminal.  

```
$ mvn clean install  
```
